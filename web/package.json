{
  "name": "mempool-atlas-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "check": "prettier --check . && tsc --noEmit",
    "format": "prettier --write .",
    "clean": "rm -rf dist coverage node_modules/.vite"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.10"
  }
}
